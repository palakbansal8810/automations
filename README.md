# automations

Install the dependencies 
```bash
pip install -r requirements.txt
```

* Every folder has its own `readme.md` file
* In facebook, you can scrape members, posts from the given groups links
* In instagram, user's profile, bio and mail can be scraped from the given category
* In telegram, you can scrape groups from specific category and active members from that groups andautomate messages